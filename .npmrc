# @react-native-community/datetimepicker declares optional peers for the
# Windows and macOS React Native forks, and npm resolves those even though this
# app builds for neither. react-native-windows then pins an older React Native
# and the install fails on a conflict that does not exist in anything we ship.
legacy-peer-deps=true
